pub const Rectangle = struct {
    left: i32,
    top: i32,
    width: u32,
    height: u32,
};

pub const Fraction = struct {
    numerator: u32,
    denominator: u32,
};

pub const Area = struct {
    width: u32,
    height: u32,
};
